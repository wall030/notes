<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.Notes" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/background_color_light</item> <!-- Light Mode -->
        <item name="android:textColorPrimary">@color/green_light</item>
        <item name="colorControlActivated">@color/green_light</item>
        <item name="cardBackgroundColor">@color/card_light_background</item>

        <item name="colorControlNormal">@color/icon_dark_color</item>
        <item name="android:textColorSecondary">@color/text_dark</item>
    </style>
    <style name="Theme.Notes" parent="Base.Theme.Notes" />

</resources>
